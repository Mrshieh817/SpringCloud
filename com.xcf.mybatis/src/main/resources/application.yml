#服务端口配置
server:
  port: 8868
  tomcat.uri-encoding: UTF-8
#数据源的配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.139.128:3306/Movie?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: Root-123
  redis:
   host: 172.16.15.149
   port: 6379
   password: null
   timeout: 300
   database: 1
#mybatis配置    
mybatis:
  mapper-locations: classpath:mappers/*.xml
  type-aliases-package: com.xcf.mybatis.Core
  configuration:
    map-underscore-to-camel-case: true
    #log-impl: org.apache.ibatis.logging.stdout.StdOutImpl   #这里打印的是mybatis返查询语句及返回数据
#logging 打印查询sql语句
logging:
  level:
   com.xcf.mybatis.Mapper: debug  # 改成你的mapper文件所在包路径
    